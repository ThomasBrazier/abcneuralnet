% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{scaler_grad}
\alias{scaler_grad}
\title{The gradient of the backward scaling transform}
\usage{
scaler_grad(z, sum_stats, method = "minmax")
}
\arguments{
\item{z}{a data frame of scaled values at which to evaluate the gradient,
typically the scaled predictive mean. Each column is treated separately.}

\item{sum_stats}{list, summary statistics learned on the training data. See \code{scaler()}.}

\item{method}{the scaling method, either \code{minmax}, \code{robustscaler}, \code{normalization}, \code{log}, \code{logit} or \code{none}.
Can be a single character (same transformation applied to all columns) or a vector of characters with one transformation per column.}
}
\value{
a data frame of gradients with the same dimensions as \code{z}
}
\description{
Returns \verb{|g'(z)|}, the absolute derivative of the backward transform applied
by \code{scaler(type = "backward")}, evaluated at the scaled values \code{z}.

This is the Jacobian factor used to carry a standard deviation from the
scaled space, where the neural network is trained, to the original parameter
scale with the delta method: \verb{sd_original ~ |g'(z)| * sd_scaled}.

The factor is a constant, and the delta method therefore exact, for the
affine methods (\code{none}, \code{minmax}, \code{robustscaler} and \code{normalization}).
For \code{log} and \code{logit} the backward transform is non-linear, so the result is
only a local linearisation around \code{z} and the resulting symmetric interval
may fall outside the support of the parameter. Prefer the conformal or
posterior-quantile intervals returned by \code{abcnn$predictions()}, which are
built by transforming interval endpoints and are exact under any monotone
scaling.
}
