% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{inv_logit_grad}
\alias{inv_logit_grad}
\title{The derivative of the backward logit transform}
\usage{
inv_logit_grad(z, a, b, n)
}
\arguments{
\item{z}{a vector of numerical values}

\item{a}{the min value of the training set}

\item{b}{the max value of the training set}

\item{n}{the number of samples in the training set}
}
\description{
The derivative of \code{inv_logit()} with respect to \code{z}. As \code{unsqueeze()} is
affine in \code{p} with slope \code{n / (n - 1)}, and \verb{d plogis(z) / dz = p * (1 - p)},
the chain rule gives \code{(b - a) * n / (n - 1) * p * (1 - p)}.
}
