# Return a data frame with hyperparameters of an `abcnn` object

The function returns all the hyperparameters of the neural network in an
`abcnn` object.

## Usage

``` r
hyperparams_abcnn(object)
```

## Arguments

- object:

  an `abcnn` R6 class object

## Value

a `data.frame` with hyperparameters
