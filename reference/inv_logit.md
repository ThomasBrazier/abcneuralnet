# The backward logit transform

The backward logit transform

## Usage

``` r
inv_logit(z, a, b, n)
```

## Arguments

- z:

  a vector of numerical values to transform

- a:

  the min value of the training set

- b:

  the max value of the training set

- n:

  the number of samples in the training set
