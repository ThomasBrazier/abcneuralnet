# Return a data frame with sample sizes of an `abcnn` object

The function returns all the sample sizes of training, testing,
evaluation, conformal prediction and observed for an `abcnn` object.

## Usage

``` r
samples_abcnn(object)
```

## Arguments

- object:

  an `abcnn` R6 class object

## Value

a `data.frame` with sample sizes
